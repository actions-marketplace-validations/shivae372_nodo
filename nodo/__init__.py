"""
Nodo — a zero-dependency codebase architecture mapper.

Scans a project, builds a dependency graph, detects code smells, and renders a
self-contained interactive HTML viewer plus machine-readable artifacts for AI
agents. Pure Python standard library — no pip install required.
"""
__version__ = "0.1.0"
